{% extends "layout.twig" %}

{% block topic %}
    Overview
{% endblock %}

{% block content %}


    <div class="panel-group" role="tablist" aria-multiselectable="true">
        <div class="panel panel-default">
            <div class="panel-heading" role="tab" id="headingOne">
                <h4 class="panel-title">
                    <a data-toggle="collapse" href="#brach-information" aria-expanded="true" aria-controls="collapseOne">
                        Table of branch information
                    </a>
                </h4>
            </div>
            <div id="brach-information" class="panel-collapse collapse in" role="tabpanel" aria-labelledby="headingOne">
                <div class="panel-body">


                    <form role="form" method="POST" action="{{ app.url_generator.generate('select.branches') }}">
                        <div class="col-md-12">

                            {% include 'issue/table.twig' with {'issues': issues, 'selection': selected_branches} %}

                        </div>
                        <div class="col-md-12 text-right">
                            <input type="submit" class="btn btn-sm btn-danger" value="delete">
                        </div>
                    </form>


                </div>
            </div>
        </div>
    </div>


    <div class="panel-group" role="tablist" aria-multiselectable="true">
        <div class="panel panel-default">
            <div class="panel-heading" role="tab" id="headingOne">
                <h4 class="panel-title">
                    <a data-toggle="collapse" href="#brach-stats" aria-expanded="true" aria-controls="collapseOne">
                        Branch statistics
                    </a>
                </h4>
            </div>
            <div id="brach-stats" class="panel-collapse collapse in" role="tabpanel" aria-labelledby="headingOne">
                <div class="panel-body">

                    {% for statsSet in stats %}
                        <div class="col-md-2">
                            {% for row in statsSet %}
                                <div><span class="label label-primary">{{ row.count }}</span> {{ row.name }}</div>
                            {% endfor %}
                        </div>
                    {% endfor %}

                </div>
            </div>
        </div>
    </div>


{% endblock %}